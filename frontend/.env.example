# Local dev — copy to .env.local
VITE_API_BASE_URL=http://localhost:3001
VITE_APP_ENV=local

# Optional — shorten reading timer for local testing (seconds). Ignored unless VITE_APP_ENV=local.
# VITE_DEV_TIMER_SECONDS=1
