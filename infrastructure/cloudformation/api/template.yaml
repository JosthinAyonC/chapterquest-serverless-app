AWSTemplateFormatVersion: '2010-09-09'
Description: ChapterQuest API — HTTP API, WebSocket API, and Lambda functions

Parameters:
  Env:
    Type: String
    AllowedValues: [dev, prod]
  LambdaArtifactsBucket:
    Type: String
  HealthLambdaS3Key:
    Type: String
    Default: auth/handlers/health.zip
  UsersGuestLambdaS3Key:
    Type: String
    Default: users/handlers/guest.zip
  LibraryListLambdaS3Key:
    Type: String
    Default: library/handlers/list.zip
  SessionsLambdaS3Key:
    Type: String
    Default: sessions/handlers/session.zip
  WsConnectLambdaS3Key:
    Type: String
    Default: ws/handlers/connect.zip
  WsDisconnectLambdaS3Key:
    Type: String
    Default: ws/handlers/disconnect.zip
  WsMessageLambdaS3Key:
    Type: String
    Default: ws/handlers/message.zip
  EnableCustomDomain:
    Type: String
    AllowedValues: ['true', 'false']
    Default: 'false'
  ApiCertificateArn:
    Type: String
    Default: ''
  CustomApiDomainHost:
    Type: String
    Default: ''
  UsersTableName:
    Type: String
  SessionsTableName:
    Type: String
  LibraryBucketName:
    Type: String
  LibraryPrefix:
    Type: String
    Default: library/

Conditions:
  UseCustomDomain: !Equals [!Ref EnableCustomDomain, 'true']

Resources:
  # --- HTTP Lambdas ---

  HealthExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ${Env}-role-function-health
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole

  HealthFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub ${Env}-function-health
      Runtime: nodejs22.x
      Handler: auth/handlers/health.handler
      Role: !GetAtt HealthExecutionRole.Arn
      Timeout: 10
      MemorySize: 128
      Environment:
        Variables:
          ENV: !Ref Env
      Code:
        S3Bucket: !Ref LambdaArtifactsBucket
        S3Key: !Sub ${Env}/lambdas/${HealthLambdaS3Key}

  UsersGuestExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ${Env}-role-function-users-guest
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDBUsersAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:PutItem
                Resource: !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${UsersTableName}

  UsersGuestFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub ${Env}-function-users-guest
      Runtime: nodejs22.x
      Handler: users/handlers/guest.handler
      Role: !GetAtt UsersGuestExecutionRole.Arn
      Timeout: 10
      MemorySize: 128
      Environment:
        Variables:
          ENV: !Ref Env
          USERS_TABLE: !Ref UsersTableName
      Code:
        S3Bucket: !Ref LambdaArtifactsBucket
        S3Key: !Sub ${Env}/lambdas/${UsersGuestLambdaS3Key}

  LibraryListExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ${Env}-role-function-library-list
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: S3LibraryRead
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - s3:ListBucket
                Resource: !Sub arn:aws:s3:::${LibraryBucketName}
                Condition:
                  StringLike:
                    s3:prefix:
                      - !Ref LibraryPrefix
                      - !Sub ${LibraryPrefix}*
              - Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:HeadObject
                Resource: !Sub arn:aws:s3:::${LibraryBucketName}/${LibraryPrefix}*

  LibraryListFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub ${Env}-function-library-list
      Runtime: nodejs22.x
      Handler: library/handlers/list.handler
      Role: !GetAtt LibraryListExecutionRole.Arn
      Timeout: 15
      MemorySize: 256
      Environment:
        Variables:
          ENV: !Ref Env
          LIBRARY_BUCKET: !Ref LibraryBucketName
          LIBRARY_PREFIX: !Ref LibraryPrefix
      Code:
        S3Bucket: !Ref LambdaArtifactsBucket
        S3Key: !Sub ${Env}/lambdas/${LibraryListLambdaS3Key}

  SessionsExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ${Env}-role-function-sessions
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: DynamoDBSessionsAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:PutItem
                  - dynamodb:UpdateItem
                  - dynamodb:DeleteItem
                  - dynamodb:Query
                  - dynamodb:BatchWriteItem
                Resource:
                  - !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${SessionsTableName}
                  - !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${SessionsTableName}/index/*

  SessionsFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub ${Env}-function-sessions
      Runtime: nodejs22.x
      Handler: sessions/handlers/session.handler
      Role: !GetAtt SessionsExecutionRole.Arn
      Timeout: 15
      MemorySize: 256
      Environment:
        Variables:
          ENV: !Ref Env
          SESSIONS_TABLE: !Ref SessionsTableName
          LIBRARY_BUCKET: !Ref LibraryBucketName
          LIBRARY_PREFIX: !Ref LibraryPrefix
      Code:
        S3Bucket: !Ref LambdaArtifactsBucket
        S3Key: !Sub ${Env}/lambdas/${SessionsLambdaS3Key}

  # --- WebSocket Lambdas ---

  WsExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub ${Env}-role-function-ws
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: WsSessionSync
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - dynamodb:GetItem
                  - dynamodb:PutItem
                  - dynamodb:UpdateItem
                  - dynamodb:DeleteItem
                  - dynamodb:Query
                Resource:
                  - !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${SessionsTableName}
                  - !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${SessionsTableName}/index/*
              - Effect: Allow
                Action: execute-api:ManageConnections
                Resource: !Sub arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:*

  WsConnectFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub ${Env}-function-ws-connect
      Runtime: nodejs22.x
      Handler: ws/handlers/connect.handler
      Role: !GetAtt WsExecutionRole.Arn
      Timeout: 10
      MemorySize: 128
      Environment:
        Variables:
          ENV: !Ref Env
          SESSIONS_TABLE: !Ref SessionsTableName
      Code:
        S3Bucket: !Ref LambdaArtifactsBucket
        S3Key: !Sub ${Env}/lambdas/${WsConnectLambdaS3Key}

  WsDisconnectFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub ${Env}-function-ws-disconnect
      Runtime: nodejs22.x
      Handler: ws/handlers/disconnect.handler
      Role: !GetAtt WsExecutionRole.Arn
      Timeout: 10
      MemorySize: 128
      Environment:
        Variables:
          ENV: !Ref Env
          SESSIONS_TABLE: !Ref SessionsTableName
      Code:
        S3Bucket: !Ref LambdaArtifactsBucket
        S3Key: !Sub ${Env}/lambdas/${WsDisconnectLambdaS3Key}

  WsMessageFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub ${Env}-function-ws-message
      Runtime: nodejs22.x
      Handler: ws/handlers/message.handler
      Role: !GetAtt WsExecutionRole.Arn
      Timeout: 10
      MemorySize: 128
      Environment:
        Variables:
          ENV: !Ref Env
          SESSIONS_TABLE: !Ref SessionsTableName
          WEBSOCKET_API_ENDPOINT: !Sub https://${WebSocketApi}.execute-api.${AWS::Region}.amazonaws.com/${Env}
      Code:
        S3Bucket: !Ref LambdaArtifactsBucket
        S3Key: !Sub ${Env}/lambdas/${WsMessageLambdaS3Key}

  # --- HTTP API ---

  HttpApi:
    Type: AWS::ApiGatewayV2::Api
    Properties:
      Name: !Sub ${Env}-chapterquest-api
      ProtocolType: HTTP
      CorsConfiguration:
        AllowOrigins: ['*']
        AllowMethods: [GET, POST, PUT, PATCH, DELETE, OPTIONS]
        AllowHeaders: ['*']

  HealthIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref HttpApi
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt HealthFunction.Arn
      PayloadFormatVersion: '2.0'

  HealthRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: GET /health
      Target: !Sub integrations/${HealthIntegration}

  UsersGuestIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref HttpApi
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt UsersGuestFunction.Arn
      PayloadFormatVersion: '2.0'

  UsersGuestRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: POST /users/guest
      Target: !Sub integrations/${UsersGuestIntegration}

  LibraryListIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref HttpApi
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt LibraryListFunction.Arn
      PayloadFormatVersion: '2.0'

  LibraryListRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: GET /library
      Target: !Sub integrations/${LibraryListIntegration}

  LibraryPreviewRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: GET /library/{key}/preview-url
      Target: !Sub integrations/${LibraryListIntegration}

  SessionsIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref HttpApi
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt SessionsFunction.Arn
      PayloadFormatVersion: '2.0'

  SessionsCreateRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: POST /sessions
      Target: !Sub integrations/${SessionsIntegration}

  SessionsGetRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: GET /sessions/{sessionId}
      Target: !Sub integrations/${SessionsIntegration}

  SessionsPatchRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: PATCH /sessions/{sessionId}
      Target: !Sub integrations/${SessionsIntegration}

  SessionsCloseRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: POST /sessions/{sessionId}/close
      Target: !Sub integrations/${SessionsIntegration}

  SessionsClaimRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: POST /sessions/{sessionId}/reviews/claim
      Target: !Sub integrations/${SessionsIntegration}

  SessionsReviewRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: POST /sessions/{sessionId}/reviews
      Target: !Sub integrations/${SessionsIntegration}

  SessionsExportRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: GET /sessions/{sessionId}/export
      Target: !Sub integrations/${SessionsIntegration}

  SessionsByCodeRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref HttpApi
      RouteKey: GET /sessions/by-code/{accessCode}
      Target: !Sub integrations/${SessionsIntegration}

  ApiStage:
    Type: AWS::ApiGatewayV2::Stage
    Properties:
      ApiId: !Ref HttpApi
      StageName: !Ref Env
      AutoDeploy: true

  # --- WebSocket API ---

  WebSocketApi:
    Type: AWS::ApiGatewayV2::Api
    Properties:
      Name: !Sub ${Env}-chapterquest-ws
      ProtocolType: WEBSOCKET
      RouteSelectionExpression: $request.body.action

  WsConnectIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref WebSocketApi
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt WsConnectFunction.Arn

  WsDisconnectIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref WebSocketApi
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt WsDisconnectFunction.Arn

  WsMessageIntegration:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref WebSocketApi
      IntegrationType: AWS_PROXY
      IntegrationUri: !GetAtt WsMessageFunction.Arn

  WsConnectRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref WebSocketApi
      RouteKey: $connect
      AuthorizationType: NONE
      Target: !Sub integrations/${WsConnectIntegration}

  WsDisconnectRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref WebSocketApi
      RouteKey: $disconnect
      AuthorizationType: NONE
      Target: !Sub integrations/${WsDisconnectIntegration}

  WsDefaultRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref WebSocketApi
      RouteKey: $default
      AuthorizationType: NONE
      Target: !Sub integrations/${WsMessageIntegration}

  WebSocketStage:
    Type: AWS::ApiGatewayV2::Stage
    Properties:
      ApiId: !Ref WebSocketApi
      StageName: !Ref Env
      AutoDeploy: true

  # --- Lambda permissions ---

  HealthLambdaPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref HealthFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${HttpApi}/*/*/*

  UsersGuestLambdaPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref UsersGuestFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${HttpApi}/*/*/*

  LibraryListLambdaPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref LibraryListFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${HttpApi}/*/*/*

  SessionsLambdaPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref SessionsFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${HttpApi}/*/*/*

  WsConnectLambdaPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref WsConnectFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*

  WsDisconnectLambdaPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref WsDisconnectFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*

  WsMessageLambdaPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref WsMessageFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*

  ApiCustomDomain:
    Type: AWS::ApiGatewayV2::DomainName
    Condition: UseCustomDomain
    Properties:
      DomainName: !Ref CustomApiDomainHost
      DomainNameConfigurations:
        - CertificateArn: !Ref ApiCertificateArn
          EndpointType: REGIONAL

  ApiMapping:
    Type: AWS::ApiGatewayV2::ApiMapping
    Condition: UseCustomDomain
    Properties:
      ApiId: !Ref HttpApi
      DomainName: !Ref ApiCustomDomain
      Stage: !Ref ApiStage

Outputs:
  ApiEndpoint:
    Value: !Sub https://${HttpApi}.execute-api.${AWS::Region}.amazonaws.com/${Env}
    Export:
      Name: !Sub ${Env}-chapterquest-api-endpoint
  WebSocketEndpoint:
    Value: !Sub wss://${WebSocketApi}.execute-api.${AWS::Region}.amazonaws.com/${Env}
    Export:
      Name: !Sub ${Env}-chapterquest-ws-endpoint
  HttpApiId:
    Value: !Ref HttpApi
  WebSocketApiId:
    Value: !Ref WebSocketApi
  HealthFunctionArn:
    Value: !GetAtt HealthFunction.Arn
